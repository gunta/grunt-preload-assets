var <%= obj.key %> = [<%_.forEach(obj.files, function(files,i) {%>
	{<%
	if (files.src) {
	  	//print('src: "' + files.src + '"' + ', ')
	}
	if (files.id) {
		//print('id: "' + files.src + '"' + ', ')
	}
	if (files.type) {
   		//print('type: createjs.LoadQueue.' + files.type + ', ')
    }
    //print(i)

	print(_.keys())

	%>},<%})%>
];